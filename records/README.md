# Runtime Records

This directory contains data generated while the simulator is running:

- `logs/`: application, authentication, and upload audit logs
- `state/`: simulated system state and command session history
- `keys/`: generated SSH host private keys
- `uploads/`: SCP/SFTP files and the `_quarantine/` directory



